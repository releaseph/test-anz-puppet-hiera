---
:backends:
        - yaml
:yaml:
        :datadir: /etc/puppetlabs/code/environments/%{::environment}/heiradata
:hierarchy:
        - roles/%{::puppet_role}
        - environments/%{::puppet_env}
        - running_on/%{::running_on}
        - common
